# Как работает дообучение (fine-tuning) Qwen в этом проекте

Объяснение для того, кто хорошо знает Python, но не знаком с ML.

---

## Зачем это вообще нужно

SpeechClip использует ASR-модель (Parakeet), которая переводит речь в текст. Проблема: английские термины она «слышит» по-русски. Говоришь «commit» — пишет «коммит». Говоришь «Cargo.toml» — пишет «карго томл».

Задача нормализатора — взять такой «испорченный» текст и вернуть термины в правильный вид, не трогая русские слова.

Это задача типа `IN → OUT`:
```
IN:  "сделай коммит и запуши в мастер"
OUT: "сделай commit и push в master"
```

Для этого берётся маленькая языковая модель (Qwen3.5-0.8B, ~800 миллионов параметров) и её дообучают именно на таких парах.

---

## Что такое модель и параметры (очень упрощённо)

Языковая модель — это по сути огромная математическая функция. Она принимает текст и предсказывает, какой токен (слово или часть слова) идёт следующим.

Внутри — миллионы чисел, которые называются **параметрами** (или весами). Именно эти числа «запоминают», как язык устроен.

**Pre-trained модель** — модель, которую кто-то уже обучил на огромном количестве текста из интернета (сотни гигабайт). Она уже «знает» язык. Мы скачиваем её готовой с Hugging Face (что-то вроде PyPI, но для ML-моделей).

**Fine-tuning (дообучение)** — мы берём эту готовую модель и немного подправляем её параметры, показывая ей конкретные примеры нашей задачи. Модель «запоминает» паттерн коммит→commit.

---

## Что такое LoRA — и почему не меняем всю модель

Полное обучение модели Qwen3.5-0.8B требовало бы изменять все ~800M параметров. Это:
- очень долго
- требует огромного GPU

**LoRA (Low-Rank Adaptation)** — хитрость: вместо изменения исходных весов модели, мы добавляем рядом с ними маленькие «накладки» (адаптеры) из новых параметров — их в 100+ раз меньше.

Аналогия на Python: представь, что исходная модель — это большой frozen dict, который нельзя менять. LoRA — это отдельный маленький dict с поправками, который применяется поверх.

Конкретно в ноутбуке:
```python
model = FastModel.get_peft_model(
    model,
    r = 16,                # размер адаптеров (чем больше — тем больше параметров)
    target_modules = ["q_proj", "k_proj", "v_proj", ...],  # какие слои адаптируем
    lora_alpha = 16,       # масштаб поправок
)
```
`target_modules` — это конкретные матрицы внутри трансформера. Именно в них происходит «внимание» (attention): модель решает, на какие части текста обращать внимание при генерации.

---

## Что такое датасет

Датасет — просто список пар `{in: "...", out: "..."}` в формате JSONL (один JSON-объект на строку).

Файл `train.jsonl` — для обучения, `eval.jsonl` — для проверки точности.

Откуда берётся датасет: генерируется автоматически через OpenAI API (скрипт `pipeline_openai.py`). GPT генерирует ~80 батчей по ~40 пар, потом другой GPT-агент проверяет их качество (QAAgent), потом локальный фильтр удаляет дубликаты и мусор. Итог — несколько тысяч пар.

Важный момент состава датасета: ~25% пар, где `IN == OUT` (чистый русский текст). Это нужно, чтобы модель не начала «исправлять» всё подряд — она должна понять, что менять нужно только термины.

---

## Формат промпта (ChatML)

Модель общается в формате диалога. Каждый пример из датасета оборачивается в «разговор»:

```
<|im_start|>system
Ты нормализуешь русскую голосовую диктовку...
<|im_start|>user
сделай коммит и запуши в мастер
<|im_start|>assistant
сделай commit и push в master
```

Это называется **ChatML** — стандартный формат диалога для Qwen.

Строка кода, которая это делает:
```python
def convert_to_chatml(example):
    return {
        "conversations": [
            {"role": "system",    "content": SYSTEM_PROMPT},
            {"role": "user",      "content": example["in"]},
            {"role": "assistant", "content": example["out"]},
        ]
    }
```

---

## Что происходит во время обучения

Обучение — это цикл:

1. Модель получает `system` + `user` часть
2. Пытается предсказать `assistant` часть (токен за токеном)
3. Сравниваем предсказание с правильным ответом — считаем **loss** (ошибку)
4. Через математику (backpropagation) понимаем, какие параметры LoRA нужно чуть сдвинуть, чтобы ошибка стала меньше
5. Сдвигаем — это один **шаг обучения**
6. Повторяем на всех парах датасета

`num_train_epochs = 2` означает: пройти по всему датасету 2 раза.

Важная деталь — `train_on_responses_only`: модель обучается предсказывать только `assistant`-часть, не тратя ресурсы на системный промпт и пользовательский ввод.

---

## Параметры обучения (гиперпараметры)

```python
per_device_train_batch_size = 4   # обрабатываем 4 примера за раз
gradient_accumulation_steps = 4   # накапливаем градиент 4 шага → реальный батч = 16
learning_rate = 2e-4              # насколько сильно сдвигать параметры за шаг
num_train_epochs = 2              # сколько раз пройти по датасету
optim = "adamw_8bit"              # алгоритм оптимизации (умный градиентный спуск)
```

`learning_rate` — аналог «скорости обучения». Слишком большой — модель скачет и не сходится. Слишком маленький — учится вечно. 2e-4 = 0.0002 — стандартное значение для LoRA.

---

## QLoRA — 4-bit квантизация весов

Qwen3.5-0.8B в полном виде занимает ~1.6 GB памяти. Google Colab T4 имеет 16 GB GPU-памяти — на первый взгляд хватает, но ещё нужна память для градиентов, батчей, оптимизатора.

**4-bit квантизация** (`load_in_4bit=True`) — веса исходной модели сжимаются: каждое число хранится не как 16-бит float, а как 4-бит int. Это в 4 раза меньше памяти. LoRA-адаптеры при этом остаются в нормальной точности (fp16) — именно их мы и обучаем.

---

## Что происходит после обучения

### 1. Merge — слияние весов

LoRA-адаптеры математически добавляются к весам исходной модели → получается одна единая модель без «накладок». Это делает вызов:

```python
model.save_pretrained_merged("qwen35_08b_norm_merged", tokenizer, save_method="merged_16bit")
```

Результат: папка в формате Hugging Face (файлы `config.json`, `model.safetensors` и т.д.).

### 2. Конвертация в GGUF

GGUF — формат файла для запуска модели через `llama.cpp` на обычном CPU (или Metal на Mac). Скрипт `integrate_model.sh` делает это автоматически:

```
HF-чекпоинт (папка) → GGUF f16 (float16, ~1.6 GB) → GGUF Q4_K_M (~500 MB)
```

`Q4_K_M` — ещё одна квантизация, теперь для инференса (не обучения). Модель занимает ~500 MB и работает на CPU.

### 3. Установка

GGUF-файл копируется в `~/Library/Application Support/dev.speechclip.oss/models/` — туда, откуда Tauri-приложение его и загружает.

---

## Полный путь данных

```
Речь пользователя
    ↓ (Parakeet ASR)
"сделай коммит и запуш в мастер"
    ↓ (нормализатор — Qwen GGUF через llama.cpp)
"сделай commit и push в master"
    ↓ (вставляется в буфер обмена)
```

---

## Полный цикл дообучения — шаги

| # | Что | Где |
|---|-----|-----|
| 1 | Генерируем датасет (`train.jsonl`, `eval.jsonl`) | `pipeline_openai.py` |
| 2 | Открываем `qwen35_08b_dictation.ipynb` в Google Colab | Colab (GPU T4) |
| 3 | Загружаем `train.jsonl` и `eval.jsonl` в Colab | Левая панель файлов |
| 4 | Запускаем все ячейки | ~10–20 минут |
| 5 | Скачиваем `qwen35_08b_norm_merged.zip` | Из Colab |
| 6 | Запускаем `integrate_model.sh` на Mac | Конвертация + установка |
| 7 | Перезапускаем SpeechClip | Приложение загружает новую модель |

---

## Используемые библиотеки

| Библиотека | Что делает |
|---|---|
| `unsloth` | Ускоренная загрузка и обучение LoRA-моделей (в 2× быстрее TRL) |
| `transformers` | Стандартная библиотека Hugging Face для работы с моделями |
| `datasets` | Загрузка и обработка датасетов (JSONL, CSV, ...) |
| `trl` | SFTTrainer — удобный враппер для supervised fine-tuning |
| `peft` | PEFT (Parameter-Efficient Fine-Tuning) — реализация LoRA |
| `bitsandbytes` | 4-bit и 8-bit квантизация весов |
| `torch` | PyTorch — фундаментальный фреймворк для работы с тензорами и GPU |
| `llama.cpp` | Инференс GGUF-моделей на CPU/Metal |
